#include <stdio.h>
/**
 *main - Entry Point
 *
 *Return: Always 0 (sucess)
 */

int main(void)
{
	int a = 0;
	char NL = 10;

	while (a < 10)
	{
		printf("%i", a);
		a++;
	}
	putchar(NL);
return (0);
}
