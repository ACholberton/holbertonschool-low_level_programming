#include "holberton.h"
/**
 *leet - encode the string into 1337
 *@rep: is a string
 *Return: string
 */
char *leet(char *rep)
{
	int a, b, replace;
replace 
	for (a = 0; rep[a] != '\0'; a++)
	{
		for (b = 0; replace[b] != '\0'; b++)
		{
			if ()
